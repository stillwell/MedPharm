<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for MedPharm ERP Android App.
  Allows cleartext traffic to localhost/emulator for development.
  In production, only HTTPS is permitted.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for Android emulator localhost (10.0.2.2) during development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
