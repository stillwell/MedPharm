<!--
  MedPharm ERP - Medical & Pharmaceutical Management System
  Copyright (C) 2026 Enlightec Ltd. (www.enlightec.com)
  Author: Robert Andrew Stillwell
  License: GNU General Public License v3.0
-->

<Window x:Class="MedPharm.Views.Pages.NewMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Message" Height="520" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource MPBg}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="New Message"
                   FontSize="20" FontWeight="Bold"
                   Foreground="{StaticResource MPText}"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="To" Foreground="{StaticResource MPTextMuted}" Margin="0,0,0,4" />
            <ComboBox x:Name="ProviderCombo"
                      DisplayMemberPath="DisplayLabel"
                      Background="{StaticResource MPSurface}"
                      Foreground="{StaticResource MPText}"
                      BorderBrush="{StaticResource MPBorder}"
                      Padding="8,6" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Subject" Foreground="{StaticResource MPTextMuted}" Margin="0,0,0,4" />
            <TextBox x:Name="SubjectInput" MaxLength="200"
                     Background="{StaticResource MPSurface}"
                     Foreground="{StaticResource MPText}"
                     BorderBrush="{StaticResource MPBorder}"
                     Padding="8,6" />
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Message" Foreground="{StaticResource MPTextMuted}" Margin="0,0,0,4" />
            <TextBox x:Name="BodyInput"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="180"
                     Background="{StaticResource MPSurface}"
                     Foreground="{StaticResource MPText}"
                     BorderBrush="{StaticResource MPBorder}"
                     Padding="8" />
        </StackPanel>

        <TextBlock Grid.Row="4"
                   Text="For medical emergencies, call 911. Secure messages are typically answered within 1–2 business days."
                   FontSize="11" TextWrapping="Wrap"
                   Foreground="{StaticResource MPTextMuted}"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,6" Margin="0,0,8,0"
                    Background="Transparent"
                    Foreground="{StaticResource MPText}"
                    BorderBrush="{StaticResource MPBorder}"
                    BorderThickness="1" Cursor="Hand"
                    Click="CancelBtn_Click" />
            <Button x:Name="SendBtn" Content="Send" Padding="16,6"
                    Background="{StaticResource MPPrimary}"
                    Foreground="#FF04121A" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand"
                    Click="SendBtn_Click" />
        </StackPanel>
    </Grid>
</Window>
